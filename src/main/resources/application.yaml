spring:
  application:
    name: Medical Record System
  datasource:
    url: jdbc:postgresql://localhost:5432/medical_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
